# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Release signing. The keystore *is* the app's identity on the Play Store: an
# attacker with it can ship an update to your users, and losing it means no one
# can ever update the app again. It lives in a password manager and in CI
# secrets, never here.
/android/key.properties
*.jks
*.keystore

# Generated code is committed on purpose, so a fresh clone builds with
# `flutter pub get && flutter run` and needs no build_runner pass first.
# Regenerate with `dart run build_runner build` after changing a route, an
# asset or a DTO; `flutter gen-l10n` after changing an .arb.
